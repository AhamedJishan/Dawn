#pragma once

#include <vector>

namespace Dawn
{
	/// <summary>
	/// Owns the main Application lifecycle, and runs the attached layer
	/// </summary>
	class Application
	{
	public:
		Application();
		~Application();

		void Run();
		
		// TODO: Add functionality for Layers

	private:


	private:
	};
}